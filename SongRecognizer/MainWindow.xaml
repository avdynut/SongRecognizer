<Window x:Class="SongRecognizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Click="OnIdentifySongButtonClick"
                Content="Identify Song"/>
        <ProgressBar Name="RecordProgress"
                     Grid.Row="1"
                     Height="20"/>
        <StackPanel Grid.Row="2">
            <Button Click="OnUpdateHistoryButtonClick" Content="Update History"/>
            <TextBox Name="Result" IsReadOnly="True"/>
            <TextBlock>
                <Hyperlink Name="Link" RequestNavigate="OnLinkRequestNavigate">
                    <TextBlock Name="LinkText"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
